{% extends 'base.html.twig' %}

{% block title %}EA-Activity New{% endblock %}

{% block body %}
    <div class="ea-wrapper">
        <h1>New Activity</h1>
        <div class="ea-form">
            {{ form_start(activityForm) }}
            {{ form_widget(activityForm) }}
            <button type="submit" class="ea-button">Save</button>
            {{ form_end(activityForm) }}
        </div>
    </div>
{% endblock %}